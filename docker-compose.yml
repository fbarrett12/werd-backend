services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: werd
      POSTGRES_PASSWORD: werd
      POSTGRES_DB: werd_dev
    ports:
      - "5433:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
